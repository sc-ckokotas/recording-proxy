"use strict";

const SimpleServer = require('../../lib/SimpleServer'),
    trySnippet = require('../support/helpers').trySnippet;

var instance = null,
    errorMessage = null;

describe("SimpleServer Class:", () => {
    describe("The constructor", () => {
        beforeEach(() => {
			instance = null;
			errorMessage = null;
		});

        describe("should accept an object as it's argument", () => {
            // it("and should throw if no arguments")
        })

    });

    describe("Each instance", () => {
        beforeEach(() => {
			
		});

    });

    xdescribe("The prototype", () => {

    });
});